import { describe, it } from "@jest/globals";

describe("FixedBinaryArray Class", () => {

    it('can be instantiated', () => {
        
    });

    it('[In Range Access] can have a bit set to 1 at index x', () => {

    });

    it('[In Range Access] can have a bit set and get at index x', () => {

    });

})